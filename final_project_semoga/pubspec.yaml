name: final_project_semoga
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_polyline_points: ^1.0.0
  mailer: ^6.0.1
  flutter_svg: ^2.0.7
  google_maps_flutter: ^2.4.0
  location: ^5.0.0
  provider: ^5.0.0
  shared_preferences: ^2.0.10
  http:
  intl: ^0.17.0
  permission_handler: ^10.4.3
  flutter_background_service: ^5.0.1
  geolocator: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/
  uses-material-design: true
